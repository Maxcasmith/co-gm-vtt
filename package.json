{
  "name": "co-gm-vtt",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "api": "pnpm --filter api dev",
    "client": "pnpm --filter client dev",
    "web": "pnpm --filter web dev",
    "migrate": "pnpm --filter api migrate",
    "migrate:down": "pnpm --filter api migrate:down",
    "migrate:rollback": "pnpm --filter api migrate:rollback",
    "migrate:fresh": "pnpm --filter api migrate:fresh",
    "seed": "pnpm --filter api seed",
    "dev": "turbo dev --ui=tui",
    "build": "turbo build"
  },
  "author": "maxsmith",
  "license": "ISC",
  "packageManager": "pnpm@10.12.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp"
    ],
    "ignoredBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "turbo": "^2.7.5",
    "typescript": "^5.9.3"
  }
}
